pipeline {
    agent any
    parameters {
        string(name: 'NAME', defaultValue: '')
        string(name: 'Ages', defaultValue: '')
    }
    stages {
        stage('Example') {
            steps {
                sh "python3 myscript.py --${params.NAME} --age --${params.Ages})"
            }
        }
    }
}
