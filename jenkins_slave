pipeline {
    agent any
    parameters {
        string(name: 'NAME', defaultValue: params.NAME)
        String(name: 'Ages', defaultValue: params.Ages)
    }
    stages {
        stage('Example') {
            steps {
                sh "python3 myscript.py params.NAME params.Ages)"
            }
        }
    }
}
